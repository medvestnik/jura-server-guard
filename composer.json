{
    "name": "jura/server-guard",
    "description": "Jura Server Guard with Jura AV Monitor for PHP hosting servers.",
    "type": "project",
    "license": "AGPL-3.0-or-later",
    "require": {
        "php": ">=8.2",
        "ext-pdo": "*"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        },
        "files": [
            "app/Support/helpers.php"
        ]
    },
    "scripts": {
        "post-autoload-dump": [
            "@php artisan package:discover --ansi"
        ]
    },
    "suggest": {
        "ext-pdo_mysql": "Required for the recommended MariaDB/MySQL production backend.",
        "ext-pdo_sqlite": "Required only for the SQLite local/small-install backend."
    }
}
